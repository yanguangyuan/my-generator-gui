package ${PackageName};


import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import ${MapperPackage}.${MapperClass};

/**
* @Author  ${Author}
* @Description  ${Remarks}
* @createTime  ${Date}
*/
@Service
public class ${ClassName}{
    @Autowired
    private ${MapperClass} ${MapperProperty};
}